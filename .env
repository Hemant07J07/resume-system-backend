# Django
DEBUG=False
SECRET_KEY=replace-this-with-a-secure-key

# Postgres (for docker-compose)
POSTGRES_DB=resume_db
POSTGRES_USER=resume_user
POSTGRES_PASSWORD=resume_pass
DATABASE_URL=postgres://resume_user:resume_pass@db:5432/resume_db

# Local DB (if not using docker)
# DATABASE_URL=sqlite:///db.sqlite3

# Webhook + optional OpenAI
WEBHOOK_SECRET=change-this-in-prod
OPENAI_API_KEY=

# Superuser (created by entrypoint if set)
DJANGO_SUPERUSER_USERNAME=admin
DJANGO_SUPERUSER_EMAIL=admin@example.com
DJANGO_SUPERUSER_PASSWORD=AdminPass123
